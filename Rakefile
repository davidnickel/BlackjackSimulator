# Add your own tasks in files placed in support/tasks ending in .rake,
# for example support/tasks/nunit.rake, and they will automatically be
# available to Rake.

# Load stack's environment.
require(File.join(File.dirname(__FILE__), 'support', 'environment'))

require 'rake'
require 'rake/testtask'
require 'tasks/vs'